# usercustomermapping

Generated by OpenAPIFuse App Export.

WARNING: This service exposes /transform without authentication. Deploy behind an API gateway, VPC, or service mesh that enforces auth at the network edge.

## Usage

```bash
mvn package
java -jar target/usercustomermapping-1.0.0.jar
```

## Environment Variables

| Variable | Description | Required | Default |
|:--|:--|:--|:--|
| `TARGET_API_BASE_URL_USERCUSTOMERMAPPING` | Base URL of the target API for mapping 'usercustomermapping' (e.g., https://api.example.com/v1) | Yes | — |
| `TARGET_API_CONNECT_TIMEOUT_MS_USERCUSTOMERMAPPING` | HTTP connect timeout in milliseconds for mapping 'usercustomermapping' | No | 3000 |
| `TARGET_API_READ_TIMEOUT_MS_USERCUSTOMERMAPPING` | HTTP read timeout in milliseconds for mapping 'usercustomermapping' | No | 5000 |
